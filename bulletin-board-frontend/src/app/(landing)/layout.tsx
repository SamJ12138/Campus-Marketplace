// Layout for SEO landing pages
// These pages are standalone and don't use the main app layout

export default function LandingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
